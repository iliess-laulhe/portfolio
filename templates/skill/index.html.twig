{% extends 'base.html.twig' %}

{% block title %}Hello SkillController!{% endblock %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-black">Ajouter un skill</h1>
        {{ form_start(form) }}
        <form>
            {{ form_widget(form) }}
            <button class="btn">OK</button>
            {{ form_end(form) }}
        </form>
    </div>
{% endblock %}
